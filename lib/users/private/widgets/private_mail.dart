import 'package:flutter/material.dart';

class PrivateMailScreen extends StatelessWidget {
  const PrivateMailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
